[general]
ui = ttyui
accounts = GMail,RIVCO,Eserre
maxsynaccounts = 1
maxage = 365

[Account GMail]
localrepository = GMail-Local
remoterepository = GMail-Remote

[Account RIVCO]
localrepository = RIVCO-Local
remoterepository = RIVCO-Remote

[Account Eserre]
localrepository = Eserre-Local
remoterepository = Eserre-Remote

[Repository GMail-Local]
type = Maildir
localfolders = ~/Mail/GMail

[Repository RIVCO-Local]
type = Maildir
localfolders = ~/Mail/RIVCO

[Repository Eserre-Local]
type = Maildir
localfolders = ~/Mail/Eserre

[Repository GMail-Remote]
type = Gmail
remoteuser = ghostbar38@gmail.com
remotepassfile = ~/.offlineimap/GMail.pass
#folderfilter = lambda foldername: foldername not in '[Google Mail]/All Mail'
realdelete = no
trashfolder = [Google Mail]/Bin
spamfolder = [Google Mail]/Spam
sslcacertfile = /etc/ssl/certs/ca-certificates.crt

[Repository RIVCO-Remote]
type = Gmail
remoteuser = joseluis@rivco.net
remotepassfile = ~/.offlineimap/RIVCO.pass
#folderfilter = lambda foldername: foldername not in '[Gmail]/All Mail'
realdelete = no
sslcacertfile = /etc/ssl/certs/ca-certificates.crt

[Repository Eserre-Remote]
type = Gmail
remoteuser = joseluis@eserre.com
remotepassfile = ~/.offlineimap/Eserre.pass
realdelete = no
sslcacertfile = /etc/ssl/certs/ca-certificates.crt

# Mailbox name recorder

[mbnames]
enabled = yes
filename = ~/.mutt/mailboxes
header = "mailboxes "
peritem = "+%(accountname)s/%(foldername)s"
sep = " "
footer = "\n"
